pydantic>=2.0,<3.0
PyYAML>=6.0
python-dotenv>=1.0
numpy>=1.26

# Milestone 7: API + web UI
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.32
httpx>=0.27
python-multipart>=0.0.9

# Milestone 8: production hardening (observability, persistence, resilience)
prometheus-client>=0.20
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp-proto-http>=1.24

# Optional: only required if EMBEDDING_PROVIDER=openai and/or LLM_PROVIDER=openai
# openai>=1.0
