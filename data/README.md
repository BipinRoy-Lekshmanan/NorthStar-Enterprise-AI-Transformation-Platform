# data/

| Folder | Contents |
|---|---|
| `processed/` | Generated by `python -m app.ingestion.pipeline`: `chunks.jsonl` (one `Chunk` per line), `errors.json` (failed files), `summary.json` (run counts). Git-ignored — regenerate locally rather than committing it. |
| `raw/`, `samples/`, `evaluation_sets/`, `sample_queries/` | Reserved for future milestones (evaluation, RAG testing); currently empty. |

See the [root README](../README.md) for pipeline configuration and usage.
